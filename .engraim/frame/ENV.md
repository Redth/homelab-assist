# ENV — this environment at a glance (hot tier)

<!-- The small, always-injected snapshot of what matters most about this project/
     environment: inventory, conventions, active quirks. Bounded on purpose — when it
     grows past ~2KB, consolidate (merge related lines into denser ones). -->

- (Nothing learned yet. EngrAIm will fill this in as you work.)
