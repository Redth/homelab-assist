# sources/ — immutable raw material

Captured web pages, search results, and pasted references — stored verbatim with
provenance (url, trust tier, fetched_at) and never edited. Facts and wiki pages are
*derived* from these; sources are the audit trail. Safe to delete only if you accept
losing that lineage.
